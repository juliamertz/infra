apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  labels:
    release: kube-prometheus-stack
  name: valheim-monitor
spec:
  podMetricsEndpoints:
    - port: metrics
  selector:
    matchLabels:
      app: valheim-server
