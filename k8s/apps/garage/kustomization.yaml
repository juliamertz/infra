apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: garage
resources:
  - namespace.yaml
  - release.yaml
  - httproute.yaml

configMapGenerator:
  - name: garage-config
    options:
      disableNameSuffixHash: true
    files:
      - garage.toml
