apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-httproute-across-namespaces
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: authelia
  to:
    - group: ""
      kind: Service
      name: theme-park 
